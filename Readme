A simple Flask API with JWT based authentication to upload an image and print the name of the image. Also, throttle the rate to 5 logins per minute using Limiter.

Copy the code to your project directory.
Pip install flask and other packages and dependencies.
Run the app.py file
Use the following link to access the login API
http://127.0.0.1:5000/login
Use userId: tay and Password: 123 
Good luck
